# 规则 — 管理后台（页面与交互）

> 产品：luckdate · 版本 V3.0 · 2026-07-27  
> **范围：仅描述管理后台的页面结构、字段与操作交互。**  
> App 侧开通 / 券核销 / Day28 等业务逻辑见 `规则-开通复购与优惠券.md` 等文档，**本文不重复。**

---

## 1. 信息架构（侧栏）

参考现有商城后台布局，luckdate 运营后台侧栏建议：

| 一级菜单 | 二级（示例） |
|----------|--------------|
| 首页 | 数据概览（可选） |
| 商品管理 | 商品列表 / 编辑 |
| 订单管理 | 订单列表 / 详情 |
| 内容管理 | （话术等内容，按需） |
| 优惠券管理 | 优惠券列表 / 批量发放列表 |
| 基础设置 | **系统配置（config 表）** |
| 用户管理 | 用户列表 / 详情 |
| 管理员管理 | 账号与权限 |

说明：

- **方案 / 是否代餐 / 复购分流** 为特殊场景，由 App 代码处理，**不做商品侧开关配置**。  
- 与方案、复购、注册送券相关的运营可调项，统一放在 **基础设置 → 系统配置**。

---

## 2. 角色与权限（页面级）

| 角色 | 可访问模块 |
|------|------------|
| 超级管理员 | 全部 |
| 运营 | 商品、订单只读或编辑、优惠券、基础设置、内容 |
| 客服 | 用户（含协助关联）、订单查看 |
| 商品 | 商品管理 |
| 管理员管理 | 仅超管 |

敏感操作（协助绑单、改 config、删券等）需二次确认，并记操作者与时间。

---

## 3. 首页（可选）

**呈现：** 卡片或简表——今日注册、今日订单、券发放/使用等汇总。  
**操作：** 点击卡片跳转对应列表（带默认筛选）。  
不在此页解释 App 业务规则。

---

## 4. 用户管理

### 4.1 用户列表

**筛选（示例）：** 注册时间、地区、是否已建档、账号状态等。  

**列表列（不要出现「已关联订单」）：**

| 列 | 说明 |
|----|------|
| 用户 ID | — |
| 昵称 / 邮箱或手机 | 脱敏展示手机 |
| 地区 | — |
| 是否已建档 | 是 / 否 |
| 注册时间 | — |
| 操作 | 查看详情 |

### 4.2 用户详情

**分区展示：**

| 区块 | 内容与交互 |
|------|------------|
| 基本信息 | 昵称、账号、地区、建档摘要、提醒时间等（只读为主） |
| **已关联订单（列表）** | 表格：订单号、商品、下单时间、履约状态等；支持多条；无则空态 |
| 优惠券 | 该用户持有券列表（券名、状态、有效期） |
| 打卡摘要 | 近几日只读摘要（可选） |
| 操作日志 | 客服/运营对该用户的人工操作记录 |

**客服操作（详情内，二次确认）：**

| 操作 | 交互 |
|------|------|
| 协助关联订单 | 弹窗输入：客户姓名 + 手机后四位 → 预览命中订单 → 确认绑定 → 刷新「已关联订单」列表 |
| 备注 | 文本备注保存 |
| 其他 | 按权限开放；**不可**在后台直接改用户打卡原始数值 |

---

## 5. 订单管理

### 5.1 订单列表

**筛选：** 下单时间、履约/支付状态、地区、订单号、客户姓名、手机后四位等。

**列表列：**

| 列 | 说明 |
|----|------|
| 订单号 | — |
| 客户姓名 | — |
| 手机 | 脱敏；支持后四位检索 |
| 商品 | 名称 / SKU |
| 支付状态 | — |
| 履约状态 | 已付款 / 已发货 / 已签收等 |
| 下单时间 | — |
| 地区 | — |
| 操作 | 查看详情 |

**不要展示「是否已被用户关联」字段。**

### 5.2 订单详情

展示订单完整信息、商品行、收件信息（权限内）。  
操作以查看为主；退款等若接入则单独按钮 + 确认。

---

## 6. 商品管理

### 6.1 商品列表 / 编辑

维护常规电商字段即可，例如：名称、价格、图文、规格、上下架、库存等。

**不要配置：**

- 是否代餐  
- 复购角色  
- 是否可用欢迎券 / 是否可用优惠券（商品侧）  

「哪些商品可走 28 天方案 / 复购推荐」由 **基础设置 config** 用产品 ID 列表指定；  
「哪些商品能用某张券」在 **优惠券** 的商品范围里配置。

---

## 7. 优惠券管理

> 配置形态对齐现有商城后台「新增优惠券」弹窗（见产品提供截图）。

### 7.1 优惠券列表

- 表格：券名称、类型、门槛/额度、有效期、启用状态、操作（编辑 / 删除）  
- 顶栏：搜索、重置、**新增优惠券**  
- 可另有「批量发放列表」二级页（发放批次、人数等）

### 7.2 新增 / 编辑优惠券（弹窗或表单页）

| 字段 | 交互 |
|------|------|
| * 优惠券名称 | 文本输入 |
| * 券类型 | 单选：**满减券** / **折扣券** / **无门槛券** |
| * 满减门槛（美元） | 数字步进（满减券时必填；无门槛可为 0 或隐藏） |
| * 减免金额（美元）或折扣 | 随类型切换：满减/无门槛为减免金额；折扣券为折扣比例或等价配置 |
| * 有效期类型 | 单选：**指定日期** / **领取后有效期** |
| 指定日期 | 选「指定日期」时：开始时间 — 结束时间 |
| 领取后有效期 | 选该项时：领取后 N 天内有效（数字） |
| 发放总数量 | 数字步进；0 可表示不限（若产品约定） |
| * 单人限领 | 数字步进，默认 1 |
| * 商品范围 | 单选：**全部商品** / **指定商品** / **包含指定商品后整单计算** |
| 指定商品 | 选后两项时：商品多选 / ID 列表 |
| 启用状态 | 开关 |

底栏：**取消** / **确认**；顶区可有 **重置** / **搜索**（列表页）。

### 7.3 与「注册欢迎券」的关系

- 欢迎券 **不是** 商品上的开关，也 **不是** 单独业务模块。  
- 在优惠券列表中正常创建一张（或多张）券 → 到 **基础设置** 把券 ID 填进「注册赠送优惠券」。  
- App 注册发哪些券，只读该 config，逻辑见 App 文档。

---

## 8. 基础设置（系统配置 / config 表）

### 8.1 配置列表页

表格列（对齐现有「系统设置」页）：

| 列 | 说明 |
|----|------|
| 配置编码 | 唯一 key |
| 配置说明 | 中文说明 |
| 配置值 | 当前值 |
| 配置单位 | 可选 |
| 操作 | **编辑** |

支持搜索、刷新。

### 8.2 三个特殊配置（必有）

| 配置编码（建议） | 配置说明 | 配置值格式 | 用途（仅说明填什么，不展开 App 逻辑） |
|------------------|----------|------------|----------------------------------------|
| `slim_plan_product_ids` | 28 天方案关联产品 | 产品 ID，多个用英文逗号 `,` 分隔 | 哪些商品 ID 视为可开通 28 天方案的关联商品 |
| `repurchase_recommend_product_ids` | 复购推荐产品 | 产品 ID，多个用 `,` 分隔 | Day28 / 复购场景推荐商品池 |
| `register_gift_coupon_ids` | 注册赠送优惠券 | 优惠券 ID，多个用 `,` 分隔 | 注册成功发放哪些券 |

**编辑交互：** 点「编辑」→ 弹窗改配置值 → 确认保存 → 列表刷新。  
校验建议：非空时 ID 为数字或约定格式；逗号分隔；去掉多余空格。

### 8.3 其他配置

可继续沿用落地页、弹窗券 ID 等通用 config（如图片中的落地页类配置），与上述三项并列维护即可。

---

## 9. 内容管理（可选）

列表 / 编辑 Sunny 话术、公告等。  
只描述后台增删改查与发布状态；话术在 App 如何触发见 App 文档。

---

## 10. 管理员管理

账号列表、角色分配、启用/停用。常规后台交互即可。

---

## 11. 页面验收清单（后台）

- [ ] 用户列表 **无**「已关联订单」列；详情内有关联订单 **列表**  
- [ ] 订单列表 / 详情 **无**「是否被用户关联」字段  
- [ ] **无**关联尝试记录菜单或页  
- [ ] 商品编辑 **无**代餐 / 复购角色 / 可用欢迎券字段  
- [ ] 优惠券新增表单含：类型、门槛、减免、有效期类型、发放量、单人限领、商品范围、启用  
- [ ] 基础设置含三项 config：方案关联产品 ID、复购推荐产品 ID、注册赠送优惠券 ID（逗号分隔）  
- [ ] 协助关联仅在用户详情，且刷新关联订单列表  

---

*规则-管理后台 V3.0 · 仅后台页面与交互 · 2026-07-27*
