# LuckDate US independent site (Next.js)

Preview (GitHub Pages):

https://leejinping8972-svg.github.io/slimAPP/us/

Local:

```bash
npm install
npm run dev
```

Static export for Pages is built in CI via `GITHUB_PAGES=true`.
