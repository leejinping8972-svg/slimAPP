# 规则 — 管理后台（页面与交互）

> 产品：luckdate · 版本 V4.0 · 2026-07-31  
> **范围：仅描述管理后台的页面结构、字段与操作交互。**  
> App 开通 / 绑定逻辑见 `规则-开通与绑定.md`，**本文不重复业务细则。**  
> **本版已去掉：商品管理、订单管理、优惠券管理。**

---

## 1. 信息架构（侧栏）

| 一级菜单 | 二级 |
|----------|------|
| 管理员配置 | 管理员列表 · 角色列表 |
| 用户管理 | 用户列表 · 用户打卡记录列表 |
| 方案列表 | 28 天方案数据看板（含实例表） |
| 系统设置 | 基础设置（config） |

说明：

- **不做**订单台账、商品运营、优惠券、激活码管理。  
- 外部查单仅作为「用户详情内的协助绑定」入口，结果写入用户关联产品；是否开通方案由基础设置中的产品 ID 判定。

---

## 2. 角色与权限（页面级）

| 角色 | 可访问 |
|------|--------|
| 超级管理员 | 全部 |
| 运营 | 用户、打卡、方案、基础设置 |
| 客服 | 用户（含协助外部查单绑定）、打卡、方案只读 |

敏感操作（协助绑定、改 config、停用管理员）需二次确认，并记操作者与时间（演示可简化）。

---

## 3. 管理员配置

### 3.1 管理员列表

列：用户名、角色、启用状态、操作（编辑 / 启用停用）。  
可新增管理员并指定角色。

### 3.2 角色列表

列：角色名、说明、权限摘要、操作。  
演示版可只读展示权限集合；正式版再做细粒度勾选。

---

## 4. 用户管理

### 4.1 用户列表

**筛选：** 注册时间、地区、绑定状态、方案状态等。  

**列：**

| 列 | 说明 |
|----|------|
| 用户 ID | — |
| 昵称 / 邮箱或手机 | 手机脱敏 |
| 地区 | — |
| 身高 / 体重 | 无则「—」 |
| 绑定状态 | 未绑定 / 已绑定 |
| 方案状态 | 无 / 待收货 / 进行中 / 已完成 |
| 注册时间 | — |
| 操作 | 查看详情 |

### 4.2 用户详情

| 区块 | 内容 |
|------|------|
| 基本信息 | 昵称、账号、地区、身高体重、提醒时间等 |
| 关联产品 | 外部查单绑定得到的产品 ID / 名称；无则空态 |
| 方案摘要 | 若已开通：当前天数、状态、起止日；链到方案列表筛选该用户 |
| 近期打卡 | 近几日只读摘要；链到打卡列表 |

**客服操作：**

| 操作 | 交互 |
|------|------|
| 协助外部查单绑定 | 弹窗：客户姓名 + 手机后四位 → 预览命中外部订单（含产品）→ 确认写入关联产品；若产品 ID ∈ `slim_plan_product_ids` 则开通/更新方案为进行中（或待收货，与 App 一致） |
| 备注 | 文本保存 |

**不可**在后台直接改用户打卡原始数值。

### 4.3 用户打卡记录列表

独立列表，支持按用户、日期、是否有记录筛选。  

**列（示例）：** 用户、日期、方案日、已记项摘要（产品/饮水/体重/睡眠/状态等）、当日生命力分、操作（查看）。  

详情只读展示当日记录明细。

---

## 5. 方案列表（看板 + 实例表）

### 5.1 数据看板

卡片示例：进行中方案数、今日打卡人数、近 7 日打卡完成率、已完成方案数。  
点击卡片可带默认筛选项落到下方表。

### 5.2 方案实例表

每一行 = 一名用户的一次 28 天方案实例。

**筛选：** 状态（进行中 / 已完成 / 待收货）、天数区间、用户昵称/ID、开始日期。

**列：** 用户、产品、开始日、当前天、状态、最近打卡日、操作（查看用户详情）。

---

## 6. 系统设置 — 基础设置（config）

| 配置键 | 名称 | 填写方式 | 用途 |
|--------|------|----------|------|
| `slim_plan_product_ids` | 28 天方案关联产品 | 产品 ID，多个英文逗号分隔 | 外部查单绑定命中这些 ID 时开通 28 天方案 |

**本版删除：** 复购推荐产品 ID、注册赠送优惠券 ID。

列表展示：键、名称、当前值、说明、编辑。编辑需二次确认。

---

## 7. 验收清单

- [ ] 侧栏无商品 / 订单 / 优惠券  
- [ ] 有管理员列表与角色列表  
- [ ] 有用户列表、用户详情、打卡记录列表  
- [ ] 有方案看板 + 可筛选实例表  
- [ ] 基础设置仅保留 `slim_plan_product_ids`（或等价项）  
- [ ] 用户详情可协助外部查单绑定；命中方案产品 ID 则开通方案  
- [ ] 不可改打卡原始值  
